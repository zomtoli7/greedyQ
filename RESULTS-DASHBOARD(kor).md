# greedyQ 결과 대시보드

[English](./RESULTS-DASHBOARD.md)

결과 대시보드는 연구자가 수집 데이터를 일상적으로 확인하는 화면입니다. Supabase는 비공개 저장 계층으로 남으며, 연구자가 직접 테이블을 찾거나 데이터베이스 쿼리를 작성할 필요가 없어야 합니다.

## 연구자가 받는 두 개의 링크

연결 및 배포가 완료되면 greedyQ는 두 개의 기본 링크만 제공합니다.

1. **설문 링크** — 참가자가 사용하는 공개 설문입니다.
2. **결과 링크** — 연구자의 Vercel 계정으로 보호되는 별도 배포입니다.

설문과 대시보드는 동일한 버전의 연구 묶음에서 생성되고 같은 Supabase 프로젝트를 사용합니다. 설문은 공개하면서 대시보드는 보호하기 위해 서로 다른 배포로 운영합니다.

## 대시보드가 보여주는 내용

초기 대시보드는 sdstudio Responses 화면의 유용한 요소인 주요 응답 통계, 응답 요약, 데이터 표, CSV 다운로드를 따릅니다. greedyQ는 실제 연구 운영에 필요한 내용을 추가합니다.

- 시작, 완료, 진행 중, 선별 제외, 동의 거부, 철회, 기술 오류 건수
- 완료율과 현재 페이지별 이탈 분포
- Direct와 Prolific 유입 구분
- 테스트와 실제 조사 구분
- 실험 조건별 무작위 배정 건수
- 문항별 응답 요약
- 내보내는 모든 열의 의미를 설명하는 변수 안내
- 응답 레코드 표
- 현재 필터가 적용된 CSV 내보내기

외부 참가자 식별자는 일반 결과 화면에 표시하지 않습니다. 이는 별도의 더 엄격한 접근 절차가 필요합니다.

## 테스트 응답은 분석 응답이 아닙니다

각 세션은 서로 독립적인 두 가지 분류를 저장합니다.

| 필드 | 값 | 의미 |
| --- | --- | --- |
| `is_test` | `true`, `false` | 실제 조사를 열기 전에 만들어진 응답인지 여부 |
| `respondent_source` | `direct`, `prolific` | 참가자가 설문에 들어온 경로 |

일반 링크 테스트는 `is_test = true`, `respondent_source = direct`입니다. Prolific으로 시험해도 테스트 응답입니다. 대시보드는 기본적으로 **실제 응답**을 열기 때문에, 연구자가 **테스트 응답**이나 **전체 응답**을 직접 선택하지 않는 한 테스트는 건수·요약·CSV에서 제외됩니다.

## 한 번의 로그인으로 끝나는 배포 경험

목표 사용자 여정은 다음과 같습니다.

1. Vercel에 로그인합니다.
2. Vercel 안에서 Supabase 리소스를 한 번 승인합니다.
3. greedyQ가 데이터베이스 생성, migration 설치, 환경변수 연결, 공개 설문 배포, 보호된 결과 화면 배포를 수행합니다.
4. 설문 링크와 결과 링크를 받습니다.

Vercel Supabase Marketplace 연동은 Supabase 프로젝트를 만들고 환경변수를 동기화할 수 있습니다. 정상 사용자 흐름에서는 연구자가 키를 복사하거나 SQL을 붙여 넣거나 Supabase Table Editor를 사용할 필요가 없습니다.

## 보안 경계

공개 설문은 Supabase URL과 publishable key만 사용하며 capability-token RPC와 row-level security로 보호됩니다. 결과 HTML에는 Supabase 비밀 키가 들어가지 않습니다. `/api/results` 서버 함수만 Vercel 환경변수에서 비밀 키를 읽고 제한된 결과를 반환합니다.

결과 배포에는 Vercel Authentication을 반드시 켜야 합니다. `results.html`과 API를 보호되지 않은 production 사이트로 공개하면 안 됩니다. 비로그인 브라우저가 거부되고 승인된 연구자는 결과를 읽을 수 있음을 확인해야 배포가 완료됩니다.

## 연구자 사용 순서

1. 결과 링크를 열고 필요하면 Vercel에 로그인합니다.
2. 실제 조사 중에는 **응답**을 **실제 응답**으로 둡니다.
3. 조사 시작 전 시험 제출을 확인할 때 **테스트 응답**을 선택합니다.
4. 테스트 필터와 별도로 **유입 경로**에서 Direct 또는 Prolific을 선택합니다.
5. 다른 탭에서 응답을 제출한 뒤 **새로 고침**을 누릅니다.
6. **참가자가 멈춘 위치**에서 예상 밖 이탈을 확인합니다.
7. 실험이라면 **조건**의 배정이 적절히 균형을 이루는지 확인하고 큰 차이가 있으면 모집을 계속하기 전에 조사합니다.
8. 응답 요약에서 잘못된 선택지나 예상 밖 값을 확인합니다.
9. **CSV 다운로드**를 누릅니다. 현재 선택한 응답 모드와 유입 경로만 파일에 포함되지만, 열에는 세션 정보와 실험 조건 및 설문에 정의된 모든 문항이 항상 포함됩니다. 참가자가 답하지 않은 문항은 빈 칸으로 남고 열 자체가 사라지지 않습니다.

## 분석 가능한 열 구조

단순 응답은 문항 ID를 열 이름으로 사용합니다. 복합 컨트롤은 읽기 어려운 JSON 한 칸으로 저장하지 않고 정해진 규칙에 따라 펼칩니다.

| 컨트롤 | 열 이름 예시 |
| --- | --- |
| Matrix | `attitudes.clarity`, `attitudes.trust` |
| Side-by-side | `comparison.current.clarity`, `comparison.proposed.clarity` |
| Rank order / constant sum | `priorities.speed`, `priorities.ease` |
| Pick, group, and rank | `items.speed.group`, `items.speed.rank` |
| Drill down | `location.level_1`, `location.level_2`, `location.path` |
| Date range | `fieldwork.start`, `fieldwork.end` |
| Timing | `reading_time.seconds_on_page` |

**변수 안내**는 각 열과 참가자에게 보인 질문, 저장 의미를 함께 보여줍니다. 응답을 받지 않는 media 전용 control은 열을 만들지 않습니다. 열 목록은 versioned survey definition에서 만들기 때문에 현재 선택한 레코드에 응답이 없어도 사라지지 않습니다.

## 필수 인수 테스트

실제 조사 전에 greedyQ는 Direct 테스트 응답을 최소 하나 만들고, 답변을 저장하고, 대시보드를 새로 고친 뒤 건수와 응답 요약이 변했는지 확인해야 합니다. Prolific 모듈을 사용한다면 완전한 Prolific 테스트 URL로 같은 검사를 반복합니다. 이 모든 테스트 동안 실제 응답 화면은 비어 있어야 합니다. 데이터베이스 행만 확인해서는 충분하지 않으며 연구자용 대시보드에 업데이트가 보여야 합니다.

현재 v0.2 대시보드는 연구 운영 검토 화면이며 통계 분석 패키지는 아닙니다. 연구자는 필터링된 CSV를 승인된 분석 절차에 사용해야 합니다.
